export default class Team {
	constructor(props) {
		this.team_id = props.team_id;
		this.name = props.name;
		this.code = props.code;
		this.logo = props.logo;
		this.league_id = props.league_id;
	}
}
